@apollo/server  //A fully-featured GraphQL server for building APIs using Apollo Server v4.

cors  //Middleware for enabling Cross-Origin Resource Sharing in Express applications.

dotenv  //Loads environment variables from a .env file into process.env.

express  //Fast, minimalist web framework for Node.js to build server-side applications.

graphql  //Core GraphQL library used to define schemas and execute queries/mutations.

graphql-tag  //Parses GraphQL queries from string literals into document nodes for use in clients/servers.

jsonwebtoken  //Used to generate and verify JSON Web Tokens (JWT) for authentication and authorization.

mysql   //Node.js driver for connecting and interacting with MySQL databases.

typeorm   //TypeScript ORM that supports MySQL and helps manage database entities, migrations, and queries.

 "@graphql-tools/merge"

 "bcrypt": "^6.0.0",


"nodemailer": "^7.0.3",

  "slugify": "^1.6.6",

   "node-cron": "^4.1.1",





   
   








  for run seeder files
   npm run seed:all

  for server running 
  npm run dev
  